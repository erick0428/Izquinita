<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Action -->
    <record id="action_variant" model="ir.actions.act_window">
        <field name="name">Variant</field>
        <field name="res_model">tindahan_pos.variant</field>
        <field name="view_mode">list,form</field> <!-- 'tree' changed to 'list' -->
    </record>

    <!-- List View -->
    <record id="view_variant_list" model="ir.ui.view">
        <field name="name">variant.list</field>
        <field name="model">tindahan_pos.variant</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_variant_form" model="ir.ui.view">
        <field name="name">variant.form</field>
        <field name="model">tindahan_pos.variant</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name" />
                        
                        
					</group>
					         
           
                </sheet>
            </form>
        </field>
    </record>
</odoo>